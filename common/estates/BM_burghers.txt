estate_burghers = { # It shows up as an error but should work still since its adding to the already existing estate_nobles vanilla file
	privileges = {
		### BM ###
		estate_burghers_promote_mercantilism
	}
}